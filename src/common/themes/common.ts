import ITheme from './ITheme';

const commonTheme: ITheme = {
  color: {
    background: '#ffffff',
    border: '#f5f5f5',
  },

  container: {
    maxWidth: 960,
  },

  spacing: {
    heading: 80,
  },
};

export default commonTheme;
