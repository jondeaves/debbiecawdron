import ITheme from './ITheme';

const commonTheme: ITheme = {
  color: {
    additional: 'lightgreen',
    primary: 'lightblue',
    secondary: 'orange',
  },

  container: {
    maxWidth: 960,
  },
};

export default commonTheme;
