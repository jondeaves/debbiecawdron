import { IStyle } from 'fela';

export interface IProps {
  title: string;
  subTitle: string;
}

export interface IStyles {
  container: IStyle;
  divider: IStyle;
  title: IStyle;
  subTitle: IStyle;
}
