import { margin, padding } from 'polished';

const style = {
  wrapper: {
    maxWidth: 960,

    ...margin(0, 'auto'),
    ...padding(0, '1.0875rem', '1.45rem', '1.0875rem'),
  },
};

export default style;
