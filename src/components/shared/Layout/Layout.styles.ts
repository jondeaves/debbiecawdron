import { IStyles } from './Layout.types';

const style: IStyles = {
  wrapper: {
    display: 'flex',
    flex: '1 0 auto',
    flexDirection: 'column',
    height: '100%',
  },

  footer: {},
};

export default style;
